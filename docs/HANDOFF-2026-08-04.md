# Session handoff — 2026-08-04

Read this before doing anything. It records what changed, what is verified, what is still open,
and **specific mistakes not to repeat**.

Start here: `state/where-we-are.md` → `state/next-actions.md` → this file.

---

## What this project is now

A folder of version-controlled files describing the Cavallo SEO project, rendered into a single
`dashboard.html` opened from disk. No server, no hosting, no scheduled jobs.

```bash
# Easiest: double-click "Cavallo Dashboard.command" in Finder
npm start          # refresh + rebuild + open
npm run build      # rebuild only, instant, needs no credentials
npm run check      # typecheck + 57 tests
```

**It was previously a Next.js app deployed to Vercel — except it was never actually deployed.**
`vercel project ls` returns zero projects; there is no `.vercel/` directory and no `*.vercel.app`
URL in the history. The nightly cron in `vercel.json` never fired once, so `@vercel/kv` was never
populated and the dashboard always served hardcoded June seed data. The app was retired this
session. All of it is recoverable from git.

---

## Verified facts (tested this session, not assumed)

### Google Search Console is NOT blocked

Three places in this repo claimed it was. All were wrong and are corrected. The Google account in
`../mrc-marketing/.env` holds **siteOwner** on `sc-domain:cavallo-inc.com`.

Measured, 12 months: **52,078 clicks across 4,629 pages**. The Ahrefs estimate the entire
disposition map was scored on claimed ~72 pages had traffic — a ~14x undercount.

### Credentials, and where they live

Google credentials are **shared** from the sibling project — never duplicated into this repo.
`lib/env.ts` maps the name differences.

| This repo needs | Comes from `../mrc-marketing/.env` | Status |
|---|---|---|
| `GOOGLE_CLIENT_ID` | `GOOGLE_OAUTH_CLIENT_ID` | ✅ works |
| `GOOGLE_CLIENT_SECRET` | `GOOGLE_OAUTH_CLIENT_SECRET` | ✅ works |
| `GOOGLE_REFRESH_TOKEN` | **`GOOGLE_ADS_REFRESH_TOKEN`** | ✅ works |

⚠️ **The trap:** MRC's own `GOOGLE_REFRESH_TOKEN` is declared but **empty**. The working token is
the one named for Ads. Despite the name its consent covers four scopes — `adwords`,
`analytics.readonly`, `tagmanager.readonly`, `webmasters.readonly` — so it reads GA4 **and**
Search Console.

In `.env.local` (gitignored, this repo only):

| Variable | Status |
|---|---|
| `AHREFS_API_TOKEN` | ✅ works — **but the subscription is being cancelled** |
| `SEMRUSH_API_KEY` | ✅ valid — Standard API + MCP entitled, verified 2026-08-04. Not yet used by any code. |
| `NOTION_TOKEN` | ❌ blank — Mark cannot create one (see below) |

### Google Ads API works — free keyword data is available

`customers:listAccessibleCustomers` returned **200 OK with 5 accounts**, using the shared MRC
credentials. So Google Keyword Planner (volumes, keyword ideas) is reachable at **no cost**. This
is the likely replacement for Ahrefs keyword research. Not built yet.

---

## Mistakes made this session — do not repeat

1. **Semrush v4 auth is `Authorization: Apikey <key>`, NOT `Bearer`.** I burned several rounds on
   `Bearer` and misdiagnosed a valid key as invalid. Base URL is
   `https://api.semrush.com/apis/v4/`. A `403` means recognised-but-not-entitled; a `404` means the
   path does not exist. That distinction is how to tell them apart.
2. **I overstated the GSC finding.** I told Mark Step 4 would "destroy real traffic." It would risk
   ~222 clicks/year. The classifier was directionally right. Do not alarm him; give him the
   aggregate before the anecdote.
3. **Removing `typescript` left the project with no type checking.** Node strips types without
   validating them. A real Notion-SDK union bug shipped and was caught only by an explicit
   `tsc --noEmit`. **Run `npm run check` before committing.**
4. **`.gitignore` had a bare `.env*`** which silently ignored `.env.local.example`. Fixed with
   `!.env.local.example`. Watch for that pattern.
5. **Node applies the LAST `--env-file` listed**, so `.env.local` must come after the shared file
   to override it. I assumed the opposite; testing corrected it.

## Do not do these

- **Never re-run `site-audit/build_disposition_map.py`.** It is retired as a generator.
  `state/pages.csv` now holds human review decisions; re-running overwrites them with heuristics.
- **Never re-run `scripts/flag-gsc-review.ts --apply`** without `--force`. It aborts on purpose —
  re-flagging cleared rows would destroy deliberate decisions.
- **Never merge measured and estimated traffic into one figure.** Conflating them is how the 14x
  error stayed invisible for months.
- **Nothing writes to Notion.** Read-only, task database only.

---

## Open questions, in priority order

### 1. The 95-row review has not started
47 classifier-flagged plus 48 GSC-flagged. This is the top of `next-actions.md` and the best
opportunity to actually teach Mark the classification logic — which was his original request.

### 2. Ahrefs is being cancelled — what replaces it?
Mark is cancelling for cost. What is lost and what replaces it:

| Data | Free replacement | Status |
|---|---|---|
| Own traffic and queries | Search Console | ✅ working |
| Keyword volumes and ideas | Google Keyword Planner via Ads API | ✅ verified, not built |
| **Competitor organic traffic** | none free — but Semrush is already paid for | ⚠️ available, unused by choice |

Competitor traffic is the only genuinely paid-only data. It is also the least load-bearing thing
in the dashboard.

**RESOLVED 2026-08-04 — Mark was right, the plan does include credits.** Both halves of the old
entry here were wrong: the key is not dead, and the v3-key hypothesis was chasing something that no
longer exists.

What is actually true:

- The existing `semrtkn-pat-…` v4 PAT in `.env.local` **works**. Verified against
  `https://mcp.semrush.com/v2/mcp`: `tools/list` returns 14 tools, and a live `domain_rank` report
  for `cavallo-inc.com` returned real data.
- **There is no separate v3 key to find.** Semrush consolidated key management. *Subscription info
  → API units* now only says "API Key has moved" and points back at the same API Keys page the PAT
  came from. Do not go looking for a 32-character v3 key.
- The earlier 403s were **per-endpoint entitlement**, not a bad key — Trends, Projects and Local are
  not on this plan. Standard API / SEO reports are.
- The plan ($279.95/mo, renews 4 Sep 2026) shows a **50,000 API unit** balance, and Semrush's MCP
  docs list 50,000-unit plans as MCP-included. Business is only required to buy *additional* units,
  not to use the API at all.

**Wired up, not in use.** The MCP server is registered and authenticated at project scope, so the
data is available on demand. As of 2026-08-04 nothing calls it and no use case has been chosen — that
is an open decision, not a rejection. See `state/decisions.md`.

Constraints to respect when using it:

- 50,000 units/month, refreshing on the renewal date, and **they cannot be topped up** on this plan.
  Billing is per line of data (~10 units/line for SEO reports), so a 50-keyword pull is ~500 units.
  Budget before building.
- MCP draws from the **same** unit pool as direct REST calls. It is not a separate allowance.
- Auth is `Authorization: Apikey <key>`. The MCP endpoint 401s with no header, and `claude mcp list`
  reporting "✔ Connected" only means reachable — not authenticated. OAuth is the other supported
  option and stores a revocable token instead of a second copy of the key.
- Semrush organic traffic is an **ESTIMATE**: 10,764/mo for `cavallo-inc.com` against 4,459
  MEASURED Search Console clicks in July 2026 — 2.4x hot. Better than Ahrefs' ~14x, still an
  estimate. Separate labelled lane, always.

### 3. The three `learn/` documents do not exist
The original request was education, not just a dashboard. The "Why" tab shows a warning saying so.
Best written *after* the review, so they address real confusion rather than guesses.

### 4. Nothing has been checked against what the client was promised
**This is a real risk.** The repo contains no record of what Carol, Maren, Crystal or Jenny were
told. Decisions made this session may contradict it:

- A **client-facing** branded portal was built, then deleted this session as internal-only
- Notion was the promised deliverable surface; that was cut
- The plan deletes or hides **787 of 1,154 pages** — roughly two-thirds of their site
- No timeline commitments are recorded anywhere

Email is **not readable from this machine** — no Apple Mail, Thunderbird caches no bodies, the
Outlook database has 0 messages, and the Google token has no Gmail scope. Mark's mail is on
`hostedemail.com`, so a Gmail scope would not help. Mark said he would gather this himself.

**When he does, create `state/client-commitments.md`** — what was promised, to whom, when — so
this can never drift again.

### 5. Notion task sync is blocked
Mark cannot create an internal integration token; that requires admin rights in the workspace
owning the task database, and it is the client's workspace. `notion-cavallo` was removed from the
MCP config; only `notion-mrc` remains and is authenticated.

**Recommendation:** move the task database into Mark's own MRC workspace. Task tracking is his
project management, not a client deliverable, and the client does not use Notion — which is the
entire premise of this session's redesign. In his own workspace he can create the token freely.

Lowest priority. The dashboard treats `state/tasks.json` as optional and renders fine without it.

---

## Environment notes

- **Subagent dispatch is unavailable.** There is no `Task` tool. Headless `claude -p` exists but
  cheap models report success without doing the work — a haiku-tier agent returned `DONE` having
  written nothing. If using subagents, use sonnet or better, and verify the diff.
- **Duplicate superpowers plugins were fixed.** `superpowers@superpowers-marketplace` (v5.1.0) is
  now disabled; `superpowers@claude-plugins-official` (v6.2.0) is active. All 14 skill names
  collided, so skill resolution was ambiguous.

## Git state

`main` is **4 commits ahead of `origin/main`** and not pushed:

```
3f7d804 feat: add one-click dashboard launcher
8de23f2 feat: add measured Search Console data; flag 48 contradicted pages
f3b2691 Add design spec: Search Console re-scoring
7d64dfe feat: share Google credentials with the sibling mrc-marketing project
```

Push with `git push origin main` when ready. Nothing depends on it being pushed.

## Design documents

- `docs/superpowers/specs/2026-08-04-project-dashboard-design.md` — why the Next.js app was retired
- `docs/superpowers/plans/2026-08-04-project-dashboard.md` — the 10-task build, all complete
- `docs/superpowers/specs/2026-08-04-gsc-rescoring-design.md` — the Search Console work
